import { Component, Input } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'pessoa',
    template: ''
})
export class Pessoa {
    id: string;
    name: string;
    register: string;
    birth: string;
    bio: string;
    photo: string;
}